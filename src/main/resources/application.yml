
spring:
  h2:
    console:
      enabled:  true
  datasource:
    url: jdbc:h2:mem:testdb
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    show-sql: true
server:
  port: 8081